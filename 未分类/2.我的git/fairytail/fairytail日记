
- 快速启动
	- nacos启动
		git clone https://github.com/alibaba/nacos.git
		cd nacos/
		mvn -Prelease-nacos -Dmaven.test.skip=true clean install -U  
		ls -al distribution/target/
		// change the $version to your actual path
		cd distribution/target/nacos-server-$version/nacos/bin
		sh startup.sh -m standalone
		sh shutdown.sh

	- prometheus启动
		- 本地方式
			prometheus --config.file=~/work/docker/prometheus/prometheus.yml
		- docker方式（数据还未挂载出）
			docker run -d --name prometheus -p 9090:9090 -v ~/work/docker/prometheus/prometheus.yml:/etc/prometheus/prometheus.yml prom/prometheus
	- grafana启动
		- docker方式
			docker run -d --restart=always --name=grafana -p 3000:3000 -v ~/data/docker/grafana:/var/lib/grafana -v ~/work/docker/grafana/grafana.ini:/etc/grafana/grafana.ini grafana/grafana
	- nacos启动
		- docker方式
			docker run -d --name=nacos_1 -p 8848:8848 -e PREFER_HOST_MODE=hostname -e MODE=standalone -v ~/work/docker/nacos/nacos_1/custom.properties:/home/nacos/init.d/custom.properties nacos/nacos-server

	- sentinal-dashboard启动
		- 本地方式
			- java -Dserver.port=8080 -Dcsp.sentinel.dashboard.server=localhost:8080 -Dproject.name=sentinel-dashboard -jar target/sentinel-dashboard.jar
			- 
	









	- Glibc：常见的桌面和服务器中的GNU/Linux类的系统中，都是用的这套C语言标准库。 其实现了常见的C库的函数，支持很多种系统平台，功能很全，但是也相对比较臃肿和庞大。
	- uClibc：一个小型的C语言标准库，主要用于嵌入式
	- EGLIBC：glibc的一种变体，目的在于将glibc用于嵌入式系统
	- Musl-libc ：Musl是一个轻量级的C标准库，设计作为GNU C library (glibc)、 uClibc或Android Bionic的替代用于嵌入式操作系统和移动设备



